class Solution(object):
    def addBinary(self, a, b):
        
        return bin(int(a,2)+int(b,2))[2:]        #firstly it convert the binary value into int and add that value and then convert that value to binary  we have used slicing because it should remove the 0b of output which is used in binary base 2 
